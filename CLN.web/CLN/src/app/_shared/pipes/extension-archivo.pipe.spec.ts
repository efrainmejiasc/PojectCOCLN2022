import { ExtensionArchivoPipe } from './extension-archivo.pipe';

describe('ExtensionArchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionArchivoPipe();
    expect(pipe).toBeTruthy();
  });
});
