/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ConteoCaracteresQuillPipe } from './conteo-caracteres-quill.pipe';

describe('Pipe: ConteoCaracteresQuille', () => {
  it('create an instance', () => {
    let pipe = new ConteoCaracteresQuillPipe();
    expect(pipe).toBeTruthy();
  });
});
