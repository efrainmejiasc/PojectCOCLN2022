import { ConteoCaracteresPipe } from './conteo-caracteres.pipe';

describe('ConteoCaracteresPipe', () => {
  it('create an instance', () => {
    const pipe = new ConteoCaracteresPipe();
    expect(pipe).toBeTruthy();
  });
});
