import { NGFormCanDeactivate } from './ngform-can-deactivate';

describe('NGFormCanDeactivate', () => {
  it('should create an instance', () => {
    expect(new NGFormCanDeactivate()).toBeTruthy();
  });
});
