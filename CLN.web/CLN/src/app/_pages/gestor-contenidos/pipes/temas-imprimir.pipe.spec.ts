import { TemasImprimirPipe } from './temas-imprimir.pipe';

describe('TemasImprimirPipe', () => {
  it('create an instance', () => {
    const pipe = new TemasImprimirPipe();
    expect(pipe).toBeTruthy();
  });
});
